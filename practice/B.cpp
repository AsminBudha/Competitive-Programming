#include <iostream>
#include <stdio.h>
#include <cmath>
#include <limits.h>
typedef long long ll;
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	freopen("input.txt","r",stdin);
	ll t;
	cin>>t;
	while(t-->0){
		ll n,m;
		cin>>n>>m;
		
	}
	return 0;
}