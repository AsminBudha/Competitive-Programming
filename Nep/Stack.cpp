#include<iostream>
#include<stack>

using namespace std;
/*int main(){
    stack<int> stack;
    if(stack.empty()) cout<<"Empty \n";
    else cout<<"Not Empty \n";

    for(int i=1;i<=5;i++){
        stack.push(i);
    }
    cout<<stack.size()<<endl;
    while(!stack.empty()){
        cout<<stack.top()<<endl;
        stack.pop();
    }
    return 0;
}
*/
